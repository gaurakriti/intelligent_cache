lask	Create REST API to handle input
pandas	Read and manage structured text data (CSV)
spaCy	Clean, tokenize, lemmatize text for better matching
redis	Fast caching with auto-expiry & user-specific logic


Relevance Scoring (Ranking System)
Instead of basic keyword in text, use:
TF-IDF score
Cosine similarity
Fuzzy matching (for typos)


 Pre-indexing with Inverted Index or TF-IDF Vectorizer
Build once during data ingestion
Speeds up search dramatically
Can be stored as pickle or persisted in DB


Automatic Cache Expiry + Refresh Logic

Frontend Dashboard (React.js or Streaml